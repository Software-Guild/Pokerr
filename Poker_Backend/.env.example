# Local PostgreSQL database. Create the database before running migrations.
DATABASE_URL="postgresql://postgres:your-local-password@localhost:5432/poker?schema=public"
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
FRONTEND_URL="http://localhost:5173"
BACKEND_URL="http://localhost:5000"
# Generate a long, random value (for example: openssl rand -base64 48).
JWT_SECRET=""
NODE_ENV="development"
